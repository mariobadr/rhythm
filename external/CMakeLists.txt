# A header-only library for parsing command line arguments.
add_subdirectory(argagg)

# A header-only library for parsing JSON files.
add_subdirectory(json)

# A header-only library for logging.
add_subdirectory(spdlog)

# A header-only library for accessing compressed streams (ZLIB).
add_subdirectory(zstr)